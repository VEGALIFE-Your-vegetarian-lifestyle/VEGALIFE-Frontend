export const crawlService = {
  jobs: async () => {
    throw new Error('Backend API contracts will be documented separately.')
  },
  dataSources: async () => {
    throw new Error('Backend API contracts will be documented separately.')
  },
}
